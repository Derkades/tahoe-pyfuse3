#!/usr/bin/make -f

clean:
	@

build:
	@

binary:
	mkdir -p debian/tahoe-upload
	mkdir -p debian/tahoe-upload/usr/bin
	cp upload.py debian/tahoe-upload/usr/bin/upload
	chmod +x debian/tahoe-upload/usr/bin/upload
	dh_gencontrol
	dh_builddeb
